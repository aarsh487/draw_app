// tailwind.config.ts
import type { Config } from "tailwindcss";

const config: Config = {
  content: [],
  theme: {
    // Your shared theme extensions
    extend: {},
  },
  plugins: [
    // Your shared plugins
  ],
};

export default config;